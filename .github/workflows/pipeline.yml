name: Deployment
run-name: Deployment Pipeline -- ${{github.event_name}} by ${{github.actor}} in ${{github.ref}}

on:
    push: 
    workflow_dispatch:

jobs: 
    deploying:
        runs-on: ubuntu-latest
        steps:
        - uses: actions/checkout@v4
        - run: npm run build
        - run: echo "Deploying code !"




