name: deployment
run-name: pushed by @${{ github.actor }}

on:
    push: 
    workflow_dispatch:

jobs: 
    deploying:
        runs-on: ubuntu-latest
        steps:
        - uses: actions/checkout@v4
        - run: echo "Deploying code !"




